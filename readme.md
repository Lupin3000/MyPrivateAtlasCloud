# MyPrivate Vagrant Cloud

## Installation and running

```shell
# git clone
$ git clone 

# change directory
$ cd

# start via docker-compose
$ docker-compose up -d

# view logs (optional)
$ docker-compose logs
```
